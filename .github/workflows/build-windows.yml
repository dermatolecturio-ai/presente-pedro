name: Build Windows exe

# Gera PresentePedro.exe em um runner Windows (você dispara do Mac via git push / Actions).
# Artifact baixável: PresentePedro-windows.zip

on:
  workflow_dispatch:
  push:
    tags:
      - "win-*"

permissions:
  contents: read

jobs:
  windows:
    runs-on: windows-latest
    timeout-minutes: 180

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          architecture: x64

      - name: Upgrade pip
        run: python -m pip install --upgrade pip wheel setuptools

      - name: Install PyTorch (CUDA 12.4 wheels; falls back in script if needed)
        run: |
          pip install torch --index-url https://download.pytorch.org/whl/cu124
        continue-on-error: true

      - name: Install torch CPU fallback if needed
        shell: pwsh
        run: |
          python -c "import torch; print(torch.__version__)" 2>$null
          if ($LASTEXITCODE -ne 0) {
            pip install torch
          }

      - name: Install project deps + PyInstaller
        run: |
          pip install -r requirements.txt
          pip install "pyinstaller>=6.0"

      - name: Download ffmpeg essentials
        shell: pwsh
        run: |
          $ScriptDir = "packaging/windows"
          $FfmpegRoot = Join-Path $ScriptDir "ffmpeg"
          $FfmpegBin = Join-Path $FfmpegRoot "bin"
          $Zip = Join-Path $env:TEMP "ffmpeg-release-essentials.zip"
          $Url = "https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip"
          Invoke-WebRequest -Uri $Url -OutFile $Zip
          $Extract = Join-Path $env:TEMP "ffmpeg-extract"
          if (Test-Path $Extract) { Remove-Item -Recurse -Force $Extract }
          Expand-Archive -Path $Zip -DestinationPath $Extract -Force
          $Inner = Get-ChildItem $Extract -Directory | Select-Object -First 1
          New-Item -ItemType Directory -Force -Path $FfmpegRoot | Out-Null
          if (Test-Path $FfmpegBin) { Remove-Item -Recurse -Force $FfmpegBin }
          Copy-Item -Recurse (Join-Path $Inner.FullName "bin") $FfmpegBin
          Test-Path (Join-Path $FfmpegBin "ffmpeg.exe")

      - name: PyInstaller build
        shell: pwsh
        run: |
          python -m PyInstaller --noconfirm --clean packaging/windows/PresentePedro.spec
          $Out = "dist/PresentePedro"
          if (-not (Test-Path "$Out/PresentePedro.exe")) { throw "PresentePedro.exe missing" }
          $OutFfmpeg = Join-Path $Out "ffmpeg"
          if (-not (Test-Path "$OutFfmpeg/bin/ffmpeg.exe")) {
            Copy-Item -Recurse -Force "packaging/windows/ffmpeg" $OutFfmpeg
          }
          Copy-Item "GUIA-PARA-O-PEDRO.md" (Join-Path $Out "GUIA-PARA-O-PEDRO.md") -Force
          @"
          Presente do Victor Prudencio para O Pedro
          =========================================
          1. Extraia este zip
          2. Abra PresentePedro.exe
          3. Leia GUIA-PARA-O-PEDRO.md
          Tudo roda LOCAL no PC (modelo no dispositivo).
          "@ | Set-Content -Path (Join-Path $Out "LEIA-ME.txt") -Encoding UTF8

      - name: Zip portable folder
        shell: pwsh
        run: |
          Compress-Archive -Path "dist/PresentePedro\*" -DestinationPath "PresentePedro-windows.zip" -Force

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: PresentePedro-windows
          path: PresentePedro-windows.zip
          if-no-files-found: error
          retention-days: 14
